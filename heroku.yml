setup:
  addons:
   - plan: heroku-postgresql
build:
  docker:
    backend: ./Dockerfile
  config:
    REQUIREMENTS_FILENAME: heroku.yml
release: 
  command:
   - ./release.sh
run:
  backend: gunicorn 'app.__main__:get_app()'