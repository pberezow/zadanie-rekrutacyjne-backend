setup:
  addons:
   - plan: heroku-postgresql
build:
  docker:
    backend: ./Dockerfile
  config:
    REQUIREMENTS_FILENAME: heroku.yml
release:
  image: backend
  command:
    - python3 ./init_db.py
run:
  backend: gunicorn 'app.__main__:get_app()'